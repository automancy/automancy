pub fn handle_transaction(entity, registry, tick_count, item_stack, source_type, direction, random) {
    if let Data::Coord(target) = entity.data.get("target")?.clone() {
        let target_coord = entity.coord.add(target.clone());

        (
            target_coord,
            item_stack,
            target,
        )
    }
}
