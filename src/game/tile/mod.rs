/// Storage and math for tile positions on a hexagonal grid.
pub mod coord;
/// Manages tile entities, which let tiles do things.
pub mod entity;
/// Handles the inventory of tiles.
pub mod inventory;
/// Manages ticking the game state.
pub mod ticking;
